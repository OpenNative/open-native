{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // The whole package could be in ES6 were it not for the hook.
    // TODO: figure out how to have a separate tsconfig just for the hook.
    "module": "CommonJS",
    "outDir": "../../dist/out-tsc",
    "rootDir": ".",
    "allowJs": true
  },
  "exclude": ["**/*.spec.ts", "**/*.test.ts", "angular"],
  "include": ["**/*.ts", "references.d.ts", "**/*.js"]
}
